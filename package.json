{
  "name": "product-catalog-service",
  "version": "1.0.0",
  "private": true,
  "description": "A simple Node.js + Express product catalog microservice with CI/CD and Docker.",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "build": "echo 'JS project - no build step'",
    "start": "node src/server.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "echo 'No linter configured'",
    "prestart": "echo 'skip build'"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0",
    "prom-client": "^15.1.3",
    "uuid": "^9.0.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "supertest": "^7.0.0",
    "vitest": "^1.6.0"
  }
}


